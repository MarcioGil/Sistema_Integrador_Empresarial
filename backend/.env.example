# Django Settings
SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (deixe vazio para SQLite em desenvolvimento)
# Para produção PostgreSQL, use formato: postgresql://user:password@host:port/database
# DATABASE_URL=postgresql://postgres:password@localhost:5432/sistema_integrador

# CORS (separado por vírgula)
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# JWT Settings (opcional - valores padrão já estão no settings.py)
# JWT_ACCESS_TOKEN_LIFETIME=60  # minutos
# JWT_REFRESH_TOKEN_LIFETIME=10080  # minutos (7 dias)

# Email Settings (SMTP)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=seu-email@gmail.com
EMAIL_HOST_PASSWORD=sua-senha-app
